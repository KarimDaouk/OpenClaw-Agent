# 2026-03-27

- User asked whether Remotion is completely free; clarified that Remotion OSS is free for local use, with possible costs only for cloud rendering or third-party assets/services.
- Recalled and confirmed prior generated presentation project name: `presentation-video-demo` at `output/presentation-video-demo`.
- Created input workspace folders for branded presentation workflow: `input/brand-assets/`, `input/reference-presentation/`, and `input/presentation-video/`.
- Opened Finder to help user upload assets locally into workspace folders.
- User uploaded brand guideline PDF: `input/brand-assets/SANS Brand Guidelines 2025 v4 1.pdf`.
- User uploaded PowerPoint reference template: `input/reference-presentation/SANS pptx template 2026 blank.pptx`.
- Confirmed previous script and avatar inputs remain the source content: `input/presentation-video/textfile.txt` and `input/presentation-video/Test AI Automation_1080p.mp4`.
- Extracted theme/media from the SANS PowerPoint template and reused those assets inside the Remotion project.
- Reworked `output/presentation-video-demo/src/PresentationVideoDemo.tsx` into a SANS-branded widescreen presentation prototype using navy/cyan styling, template imagery, branded header/footer treatment, and presenter-avatar layout.
- Started Remotion Studio live server for the branded presentation at `http://localhost:3000` (network URL observed: `http://192.168.0.66:3000`).
- User specifically noticed that the large background “SANS GIAC” wordmark looked awkwardly cut off; adjusted the background logo treatment to be subtler and repositioned with a softer bottom fade.
- Committed the branded presentation update in git with commit `160afbe` and message: `Brand presentation demo with SANS styling`.
- Developed a reusable presentation skill based on SANS/Remotion layout work: `skills/presentation-layout-director/`, packaged as `dist/presentation-layout-director.skill`.
- Durable presentation lessons from this workflow: user prefers live-server review before rendering, branded layouts over generic ones, smoother section-based changes over noisy movement, and opposing avatar/content layouts for variety without clutter.
- Durable technical lessons from this workflow: when embedded avatar playback looks choppy in rendered output, first isolate the avatar statically; the best fix was an overlay-sized avatar asset with easier decode settings and frequent keyframes rather than only tweaking motion curves.
- Durable implementation lesson: for mirrored presentation layouts, explicit panel widths and coordinates are more reliable than partial grid flipping when avoiding overlap/off-canvas issues.
- User explicitly wants these presentation transitions memorized and reused in future work:
  - Safe side-switch transition: avatar moves from one side to the other, content and visual panels switch accordingly, smooth/professional, good for structure changes.
  - Section-based slide transition: change layout only at meaningful moments rather than every slide; cleaner and less chaotic.
  - Hero avatar transition: avatar zooms in for emphasis during a switch, then settles into the new side.

# 2026-03-28

- User clarified preferred presentation transition naming: call the midpoint avatar zoom + stage side swap a **hero side-switch transition**.
- Definition to reuse: avatar stays parked, performs one hero zoom transition at the middle of the presentation, then settles on the opposite side while the content switches sides at the same moment.
- Updated `output/presentation-video-demo/src/PresentationVideoDemo.tsx` accordingly and committed the fix as `8385e36`.
- Rebuilt the SANS-branded Remotion presentation around a new 2:25 timed script and a longer avatar source video; updated all 11 slides, timings, and composition duration. Commit: `6b7ed50`.
- User preference reinforced: content should stay stable except for a single meaningful midpoint stage switch; avatar should remain parked except during the hero side-switch transition.
- Added legal royalty-free music options for presentation work in `input/presentation-assets/music/`: `Airport Lounge.mp3`, `Inspired.mp3`, and `Carefree.mp3` (Incompetech). User auditioned tracks inside the presentation and currently switched to `Carefree`. Relevant commits: `5b1f2ea`, `b1b3b8c`, `9bce066`, `e8df35b`.
- Avatar presentation preferences for future decks: use a larger presenter treatment, but avoid covering slide material; user wanted a more portrait feel, then later wanted the avatar to blend into the background without any visible rectangular frame.
- Green-screen workflow now established for this project: processed `Longer_Script_Version_for_AI_Testing.mp4`, keyed out the green background, then moved toward a circular alpha overlay to remove a persistent visible rectangular box. Key commits: `ba6fd33`, `a8a085c`, `6779d4d`, `f539438`.
- Bullet animation preference: points should appear one after another with visible motion, not too fast; user preferred a slightly slower stagger so the animation reads clearly. Commit: `ec4ef4b`.
- Important avatar render lesson: the choppy export issue was finally fixed by switching the presenter clip from `Video` to `OffthreadVideo` in Remotion while using a pre-keyed, pre-cropped stable MP4 asset (`avatar-keyed-cropped-stable.mp4`). Heavy alpha/circle video exports were unreliable for smooth final renders. The stable solution was: pre-process the avatar into a simpler keyed/cropped MP4, render it via `OffthreadVideo`, and contain it inside a circular presenter matte in the component rather than relying on heavyweight alpha-video playback. Key commits in the path to the final fix: `1d6c178`, `b57ca75`, `f177f7e`.
- Important loop animation lesson: browser-animated SVG loops looked fine on the live server but exported at the wrong speed. The correct fix was to replace those self-animated SVG assets with Remotion-native, frame-driven React/SVG motion components inside `PresentationVideoDemo.tsx` so preview and export timing match exactly. This applied to both whitespace loops and visual-panel loops. Key commit: `af69ae0`. Also refined the process/workflow visual so node labels sit visibly below nodes and below the body-text area. Key commits: `bd319d1`, `d8551b4`, `0b6183e`.

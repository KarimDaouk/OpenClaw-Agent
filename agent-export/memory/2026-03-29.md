# 2026-03-29

- User approved a new transition name: **Side-swap transition**.
- Defined it as a coordinated transition where the avatar placement and slide layout swap sides together without the hero zoom treatment.
- User clarified an important motion rule: transition direction should be chosen from the stage/content state at that moment in the video, not assumed to always move left-to-right or right-to-left.
- Long-term memory updated to keep the official transition names and this context-driven direction rule.
- User confirmed the correct transition logic rule after reviewing the preview: transitions must derive from the avatar/content's current stage position at that exact moment, not from hardcoded left/right assumptions.
- Active presentation transition set now includes:
  - **Hero side-switch transition**: hero zoom from the current side, landing on the opposite side with content swapping accordingly.
  - **Side-swap transition**: coordinated avatar + content side swap without hero zoom.
- New transition concept added for testing in `output/presentation-video-demo`: avatar moves to the top center, with presentation content beneath it. User iterated the layout to remove arrows, keep the lower content side-by-side, center the avatar horizontally between the lower content blocks, enlarge the presentation content, and nudge it right for balance.
- User approved the official name **Top-center transition**.
- Transition rule reinforced again: avatar movement must always depend on where the content/stage is at that moment, not on hardcoded directional assumptions.

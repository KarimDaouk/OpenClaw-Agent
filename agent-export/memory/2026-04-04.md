- Built a local video job API under `api/` for OpenClaw workspace integration with endpoints for create/status/log/files/run, later adding in-place resubmit (`POST /jobs/:jobId/resubmit`) and explicit render (`POST /jobs/:jobId/render`).
- Fixed the job runner architecture by switching from brittle CLI dispatch attempts to the OpenClaw Gateway HTTP API (`/v1/responses`) with gateway token auth loaded from `~/.openclaw/openclaw.json`.
- Enabled Gateway HTTP endpoints in `~/.openclaw/openclaw.json` under `gateway.http.endpoints.responses.enabled=true` and `chatCompletions.enabled=true`, then restarted the gateway.
- Shifted the API contract to preview-first: generation jobs should stop at preview-ready and return `previewUrl`; final MP4 rendering should only happen when explicitly requested.
- Committed workspace changes:
  - `c33dc95` Add starter OpenClaw video job API
  - `e4de07b` Add in-place job resubmit endpoint
  - `a6708b5` Make video jobs preview-first with explicit render action
- Confirmed a real website-driven video job `df8e7c88-152a-45db-851b-054430928152` processed successfully through the API/Gateway path, producing project `output/visitor-registration-sop-bastien-20260404`, preview URL `http://localhost:3001`, and rendered file `output/visitor-registration-sop-bastien-20260404/renders/visitor-registration-sop.mp4`.
- Confirmed a successful test/resubmit job `aaabc1ed-e1f5-4444-bea6-cadbdb3edcac` completed with planning outputs after in-place overwrite/regenerate flow.

- Worked extensively on the local video-job API and simplified its contract step by step based on user feedback. Final API shape: `POST /jobs` creates and runs immediately with payload `{ prompt, avatarSource?, renderRequested? }`; no prompt snapshot files are persisted locally, only minimal job state. Relevant commits made today included: `de6a3c4` (single prompt input), `a2ace29` (avatar source field), `1cf315b` (always run on create), and `0b58e1d` (force `x-openclaw-model: openai-codex/gpt-5.4`).
- User wants the website backend to keep the canonical production prompt in `api/initiation-prompt.md`, replace placeholders there, and send the final resolved `prompt` plus `avatarSource` to the local API.
- Diagnosed a failed job (`26a58cf5-cbc1-483f-b30e-b4905295e123`): actual failure was upstream rate limiting (`⚠️ API rate limit reached. Please try again later.`), while the backend incorrectly exposed the HeyGen avatar MP4 as `status.previewUrl`, causing the UI to look like a preview existed when it did not. Recommended fix: never treat `avatarSource` as a preview URL and mark rate-limit failures more explicitly.
- User iterated heavily on the large production prompt for presentation generation. Current preference: strong intro stating it is creating a professional PowerPoint-like presentation in Remotion, explicitly tell it to use available local skills, remove the old Delivery Contract and Final Report sections, remove `input/reference-presentation/`, simplify inputs to `{{PROJECT_NAME}}`, `{{AVATAR_SOURCE}}`, and `{{PRESENTATION_OUTLINE}}`, and keep timing adherence described as part of the presentation outline/timings.

- Investigated local video-job API behavior with openclaw-control-ui jobs. Durable pattern: job tracker can remain stuck at `dispatching` or `gateway-timeout` while the underlying Remotion project continues building in the background and may become fully previewable later.
- Confirmed two timeout modes worth remembering:
  1. partial scaffold only (e.g. output folder initially had mostly `package.json` and cache)
  2. later reconciliation-worthy case where the project matured into a full Remotion app with `src/`, `public/`, deck component files, and live preview despite stale job status.
- For project `output/1st Test`, later observed full project contents and active preview despite job `036bcac5-ad38-42f1-b440-c4d1e75cf998` still reporting `running` / `dispatching` / `5%`.
- Port mapping observed during investigation:
  - `3000` served `1st Test`
  - `3001` served `presentation-video-demo`
  - `3003` was the website/dashboard
  - `3004` was later listening as an additional preview-related port
- Important diagnosis to preserve: preview server existence alone is not proof of successful generation; structural completeness of the project folder must also be checked before marking preview-ready.
- Strong recommended fix direction repeatedly identified: add job-state reconciliation based on actual filesystem/project completeness plus matching live Remotion preview server, so `GET /jobs/:jobId` can heal stale `dispatching` / `gateway-timeout` states into `preview_ready` when appropriate.
- Also noted that active `remotion render` work can make preview feel broken/sluggish even when the preview server itself is healthy and returning HTTP 200.
